# ASR/VAD model weights — host-provided, not committed.
# VAD onnx (silero_vad.onnx) と ASR gguf を data/models に揃えて置く（MODELS_DIR）。
*.gguf
data/models/

# GGUF 変換ヘルパ（scripts/convert_ja_gguf.sh）の作業用 clone / venv
.cache/

# Python のビルド/パッケージング成果物
__pycache__/
*.py[cod]
*.egg-info/
/dist/
/build/
.venv/

# WebUI のランタイム状態（data/state.json を使う）
state.json
